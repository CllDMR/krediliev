import { FC, useState } from "react";

export const FavoriteTab: FC<{}> = ({}) => {
  const [openTab, setOpenTab] = useState(1);
  return (
    <>
      <div className="container mx-auto mt-8">sadasdas</div>
    </>
  );
};
